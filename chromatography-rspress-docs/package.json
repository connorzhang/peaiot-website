{
  "name": "chromatography-rspress-docs",
  "version": "1.0.0",
  "description": "Dummy package to trigger root build for Baota Webhook",
  "scripts": {
    "build": "cd .. && npm install --include=dev --registry=https://registry.npmmirror.com && (npm run build:doc > build.log 2>&1 || true) && mkdir -p build_doc && cp build.log build_doc/build.txt || true"
  }
}
